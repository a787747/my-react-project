# INCIDENT 2026-08-12 — Production EPE database and n8n

Captured: 2026-08-12, approximately 20:40–20:45 UTC.  
Host: `3569961-foreignpay.twc1.net` (`92.51.45.147`).  
This report uses only commands run in this session. Items not proven here are marked **unverified**.

---

## Three answers

1. **Is the data intact?** Yes, as far as this session can measure. `performance_db` is present, queryable, and internally consistent. Live row counts match a verified `pg_dump` taken in this session. Timestamped tables show **no writes after 2026-07-10**. All 234 evaluations sit in December 2025. This does not prove the absence of unlogged SELECT access.

2. **Did anything fire?** No schedule/cron/interval workflow is active, so nothing could fire on a timer. After n8n was started in the previous session (2026-08-12 19:22:45 UTC), seven **webhook** executions ran, all `success`. Two were this workstation’s login probes. Five others at 19:23:25 UTC look like a logged-in portal session. Client IPs for those five are **unverified** (`execution_metadata` is empty).

3. **Was anyone else in?** **Cannot be proven either way for historical Postgres sessions.** `log_connections` is off, `logging_collector` is off, and `log_line_prefix` has no client host. Right now `pg_stat_activity` shows only Docker-internal n8n (`172.18.0.5`) plus this session’s local `psql`. The same port is under active credential stuffing, including username `admin`. No `performance_db` row timestamps moved after the July stop.

---

## Access path (contradiction resolved)

Public-key SSH with `~/.ssh/id_ed25519` as `root@92.51.45.147` failed this session: `Permission denied (publickey,password)`.

The previous session did **not** use a working SSH key. It used **password authentication as `root`**, via the already-saved IntelliJ/WebStorm SSH config for this host (`sshConfigs.xml`: `authType="PASSWORD"`, `username="root"`). That is the path reused here. DBeaver’s encrypted store was not decrypted. No other password stores were searched.

Control: multiplexed SSH as `root` returned `ACCESS_OK`, hostname `3569961-foreignpay.twc1.net`.

The credential that unblocks this host if the Mac item is missing: **SSH password for `root@92.51.45.147`**. It is not written here.

---

## What is running

Docker on this host (this session, `docker ps -a` + `docker inspect`):

| Name | Image | Status (this session) | Published ports | Notes |
|------|-------|----------------------|-----------------|-------|
| `n8n-n8n-1` | `n8nio/n8n:latest` | Up since 2026-08-12 19:22:45 UTC | `0.0.0.0:5678` | EPE API + other apps. Restart policy `on-failure`. |
| `postgres_n8n` | Postgres 17.0 | Up since 2026-07-10 03:11:53 UTC | `0.0.0.0:5432` | Contains **n8n public schema and `performance_db`**. |
| `postgres_main` | Postgres 17.0 | Up since 2026-07-10 03:11:53 UTC | `0.0.0.0:5431` | **No** `performance_db`. Not owned by this investigation. |
| `portainer` | `portainer/portainer-ce:latest` | Up since 2026-07-10 03:11:53 UTC | `0.0.0.0:8000`, `0.0.0.0:9000` | Not owned by EPE. |
| `redis_prod` | `redis:latest` | Up since 2026-07-10 03:11:53 UTC | none on host | Internal only. |

n8n version from container logs this session: **1.121.3**. Editor HTTP 200 at `http://92.51.45.147:5678/`.

This VPS is shared. Same Postgres also has schemas `medical_equipment` (3 tables) and `new_scheme` (6 tables). Same n8n has active non-EPE webhooks (`school-helper`, `new_clinic/*`). Host name is `foreignpay`. Do not assume exclusive ownership.

---

## Why n8n was stopped (mechanical cause)

Verified in this session:

- `who -b` / `last -x reboot`: **system boot 2026-07-10 06:11** (host local, UTC+3) = **03:11 UTC**.
- n8n last logs before this session’s start: `Received SIGTERM. Shutting down...` then `Stopping n8n...`, `ExitCode=0`, `FinishedAt=2026-07-10T03:11:30Z`.
- Sibling containers `StartedAt=2026-07-10T03:11:53Z`.
- n8n `RestartPolicy=on-failure`. Exit 0 after SIGTERM does not qualify as a failure, so Docker did not bring it back after the reboot.

There were additional reboots the same morning (`01:32` and `02:50` local). **Who or what initiated the reboot is unverified.**

---

## Step 2 — Trigger inventory (taken before any change)

Restore list (full JSON, captured 2026-08-12 20:41:40 UTC from `postgres_n8n.public.workflow_entity`):

`docs/n8n_workflow_state_2026-08-12.json`

| State | Count |
|-------|-------|
| Total workflows | 58 |
| Active, not archived | 40 |
| Inactive, not archived | 9 |
| Archived (all inactive) | 9 |

Node types across all workflows include exactly **one** `n8n-nodes-base.scheduleTrigger`. No `cron` or `interval` node types exist.

That schedule workflow:

| id | name | active | archived | rule |
|----|------|--------|----------|------|
| `94rbRP9YdzpKZF4n` | Подкачка кэша в Redis | **false** | false | interval minutes |

It was already inactive before this session. Confirmed again after inventory: `active=f`.

Active trigger types: `n8n-nodes-base.webhook` only (plus `respondToWebhook`, which is not a trigger). Two inactive workflows have `manualTrigger`; one inactive has `executeWorkflowTrigger`.

Registered webhook paths (`public.webhook_entity`, 44 rows) include the EPE API surface (`auth/login`, `api/*`, `admin/*`, `manage-criteria`, …) and non-EPE paths `school-helper`, `new_clinic/equipment`, `new_clinic/request`, `new_clinic/rooms`. Live webhook nodes have `authentication` **null** (treated as none). They are reachable without n8n-level auth.

---

## Step 3 — Protective pause

**Skipped.** No active schedule/cron/interval trigger was found. Nothing was deactivated. The file above remains the restore list.

---

## Step 4 — Backup (verified from the dump file)

File: `backups/performance_db_2026-08-12.sql` (gitignored).

Taken with `docker exec postgres_n8n pg_dump -U admin -d postgres -n performance_db --no-owner --no-acl`.

| Check | Result from the file |
|-------|----------------------|
| Size | 125 640 bytes (123K) |
| Header | `PostgreSQL database dump`; dumped from **17.0** |
| Footer | `PostgreSQL database dump complete` |
| `CREATE TABLE` | **12** |
| `COPY` tables | **12** |
| `INSERT` statements | 0 (data is COPY) |

Row counts parsed from `COPY ... FROM stdin` blocks in the dump (not from live SQL):

| table | rows in dump |
|-------|----------------:|
| criteria | 8 |
| departments | 14 |
| email_verification_codes | 69 |
| evaluation_periods | 1 |
| evaluation_scores | 644 |
| evaluations | 234 |
| global_settings | 0 |
| grades | 10 |
| invite_tokens | 6 |
| score_coefficients | 80 |
| score_corrections | 3 |
| users | 73 |
| **total COPY rows** | **1142** |

The dump contains application data, including `users.password_hash`. Do not commit it.

---

## Step 5 — Counts and integrity (live, read-only)

Exact live counts this session matched the dump table-for-table (73 users, 234 evaluations, 8 criteria, 644 scores, 1 period, …).

### Periods

One row in `performance_db.evaluation_periods`:

| id | name | is_active | start_date | end_date |
|----|------|-----------|------------|----------|
| 1 | Annual Review 2025 | true | 2025-01-01 | 2025-12-31 |

Periods are distinguishable (single named period). There is **no H1-2026 period**.

### Timestamp extrema (stored `timestamp without time zone`; timezone **unverified**)

| column | min | max | rows ≥ 2026-07-10 |
|--------|-----|-----|------------------:|
| users.created_at | 2025-12-08 18:35:59 | 2025-12-23 18:24:35 | 0 |
| evaluations.updated_at | 2025-12-24 04:21:06 | 2025-12-26 19:25:27 | 0 |
| email_verification_codes.created_at | 2025-12-23 04:44:38 | 2025-12-26 06:20:55 | 0 |
| invite_tokens.created_at | 2025-12-15 09:49:35 | 2025-12-23 06:16:45 | 0 |
| score_corrections.created_at / updated_at | 2025-12-25 10:07:13 | 2025-12-26 18:15:12 | 0 |

All 234 evaluations have `updated_at` in **2025-12**. Zero evaluations in calendar H1 2026.

`users.join_date` ranges 2020-01-01 .. 2020-06-01 (0 joiners in H1 2026). That range looks like placeholder data; **unverified**.

Tables with **no** created/updated columns: `criteria`, `departments`, `grades`, `evaluation_scores`, `score_coefficients`, `global_settings`. Last-write time for those is **unverified** except that `evaluation_scores` are FK-tied to the December 2025 evaluations.

---

## Step 6 — Exposure

### Reachability now

This workstation public IPv4: `212.36.169.90`.  
`nc` to `92.51.45.147:5432` succeeded. Postgres handshake: SCRAM-SHA-256, SSL response `N` (no SSL). Settings: `listen_addresses=*`, `ssl=off`, `password_encryption=scram-sha-256`.

`postgres_n8n` publishes `HostIp=0.0.0.0 HostPort=5432` (and `::`). `ss` shows `docker-proxy` listening on `0.0.0.0:5432`.

### How long has 5432 been open to the internet?

**Cannot determine.** What is verified:

- The container has been running with that publish mapping since **2026-07-10 03:11:53 UTC**.
- The container was **created 2024-10-06** with port 5432 published.
- A Timeweb/cloud firewall in front of `0.0.0.0` is **unverified**.
- It is reachable from the internet **today**.

### `pg_hba.conf` (via `pg_hba_file_rules`)

- `local all all trust`
- `host all all 127.0.0.1/32 trust`
- `host all all ::1/128 trust`
- `host all all all scram-sha-256`  ← any remote address may attempt login

### Roles

Login-capable role in this cluster: **`admin` only**, and it is superuser (`rolsuper=t`, `rolcanlogin=t`). There is no `postgres` role. Schema `performance_db` owner is `admin`. Table grants in that schema are only to `admin`.

### Current sessions (`pg_stat_activity`)

- Background workers started 2026-07-10 03:11:54 UTC.
- `admin@postgres` from `172.18.0.5:49404` idle `SELECT 1` (n8n on Docker network `n8n_default`).
- This session’s `docker exec` psql (no `client_addr`).
- **No other client_addr.**

### Connection logs / other source addresses

`log_connections=off`, `log_disconnections=off`, `logging_collector=off`, `log_min_messages=warning`, `log_line_prefix='%m [%p] '` (no `%h`). Successful connections are not recorded. Client IPs are not in these log lines.

`docker logs postgres_n8n` contained **3 291 953 `FATAL` lines** when counted this session (command then stopped; full unique-user pass over 1.6 GB json log was not completed). Container json log size: **1 710 302 256 bytes**.

Last 8 MB of that json log (this session) failed-auth usernames included at least: `postgres` (8031), `admin` (330), `root` (296), `test` (268), `app` (159), plus `webuser`/`web`/`user`/`postgresql`/`postgre` (148 each). That is internet scanning, including the real role name `admin`.

Whether any remote `admin` login ever succeeded: **unverified**.

n8n webhooks have no n8n authentication. After restart, seven webhook executions exist; IPs **unverified**.

Other internet listeners on this host, not changed: `22` (sshd), `5431` (`postgres_main`), `5678` (n8n), `8000` and `9000` (Portainer).

---

## What this session changed

- **Data:** none.
- **Schema:** none.
- **n8n workflow active flags:** none (step 3 skipped).
- **Firewall / pg_hba / containers:** none.
- **Repo:** `backups/` added to `.gitignore` so the dump cannot be committed; dump file written; this report and `docs/n8n_workflow_state_2026-08-12.json` written.

---

## Immediate risks (not remediated; out of scope)

1. Postgres `5432` published on `0.0.0.0` with `host all all all scram-sha-256`, superuser `admin`, SSL off, under active brute force.
2. n8n webhooks, including `auth/login` and destructive EPE admin routes, have no webhook authentication.
3. n8n `RestartPolicy=on-failure` will drop the API again on a clean shutdown/reboot.
4. EPE data shares a cluster with other product schemas and a publicly published second Postgres on `5431`.

Closing `5432`, editing `pg_hba.conf`, or stopping foreign containers was explicitly out of scope.
