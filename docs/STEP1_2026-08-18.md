# Step 1 — Container Foundation and New Database

**Date:** 2026-08-18  
**Host:** `92.51.45.147`  
**Result:** completed  
**Guard design buildable on this n8n instance:** **YES**

No workflow was activated. The n8n Postgres configuration still points to
database `postgres`; the new `epe_2026` database is unused.

## 0. SMTP preflight

A temporary, inactive manual-trigger workflow sent a real email through the
live `SMTP account` credential to `alexander@sedamedical.com`.

Evidence:

- n8n execution exit code: `0`
- delivery confirmed by Alexander
- temporary workflow deleted
- original short-lived API-key workaround reverted

Rollback: none required. The workflow was not activated and was deleted.

## 1. Recovery state

### Stack and container capture

- Sanitized live Portainer stack: `infra/n8n-stack.yml`
- Pre-change container inspect:
  `infra/n8n-container-inspect-2026-08-18.json`
- The inspect file is covered by
  `.gitignore` rule `infra/n8n-container-inspect-*.json`.
- Exact pre-change stack retained on the host at
  `/root/backups/epe-step1-2026-08-18/n8n-stack.before.yml`.
- Safe rollback stack retained at
  `/root/backups/epe-step1-2026-08-18/n8n-stack.rollback.yml`.

The repository stack uses placeholders for the Postgres password,
`N8N_ENCRYPTION_KEY`, and `JWT_SIGNING_SECRET`. No secret value was written to
tracked project files or this report.

### Secret custody

The existing encryption key and the new signing secret were stored in the
local macOS Keychain and verified by round-trip reads:

- `EPE n8n encryption key 92.51.45.147`
- `EPE JWT signing secret 92.51.45.147`
- account: `alexander@sedamedical.com`

### Dumps

Gitignored files:

- `backups/2026-08-18-step1/performance_db_2026-08-18.dump`
  - SHA-256:
    `e66b97caaf1825b7902dacad25ab3f2cac983b7dc6be0c873d0a2f468864676e`
- `backups/2026-08-18-step1/n8n_public_2026-08-18.dump`
  - SHA-256:
    `e8f60a6279aea7bf9822383061e427d8b57e700d5a2c3fad039b7279bc764381`

Both dumps were restored together into a throwaway database:

- `performance_db`: 12 tables, 1,142 rows; all table counts and object counts
  matched
- n8n `public`: 52 tables, 2,121 rows; all table counts and object counts
  matched
- restore exits: `0`, `0`
- throwaway database dropped and confirmed absent

Rollback: use these dumps only if data damage or credential unreadability is
proven. Do not restore a healthy database for a cosmetic/runtime check.

## 2. Immutable n8n image

The running container used local image ID:

`sha256:b18bb0e3663368e76886f9220aa663ebe0737fe0cf90464b4a9da517515070b0`

Its registry digest was:

`n8nio/n8n@sha256:0a65e6e5995c19e0cf7e83d6b08ffa6c1898e8a53ff1658e6e7b22e68576c673`

Evidence:

- `RepoDigests` contained the digest above
- `docker pull` by digest succeeded
- the pulled image ID exactly matched the running image ID
- n8n reported version `1.121.3`

The mutable `1.121.3` tag was not used because it currently resolves to a
different image ID. The security-update freeze and revisit point are recorded
in `DECISIONS.md`.

Rollback: use `n8n-stack.rollback.yml`, which is also pinned to the verified
digest. Do not deploy `n8n-stack.before.yml` directly because it contains the
unsafe `latest` reference.

## 3. Container recreation

Added operator-set variables:

```text
NODE_FUNCTION_ALLOW_BUILTIN=crypto
NODE_FUNCTION_ALLOW_EXTERNAL=jsonwebtoken
N8N_BLOCK_ENV_ACCESS_IN_NODE=false
JWT_SIGNING_SECRET=<64 random bytes, base64>
```

The signing secret has 64 decoded bytes and a base64 length of 88. Its value
was not printed or stored in workflow JSON.

Final recreation evidence:

- 11 pre-existing operator-set variables compared before/after
- missing operator variables: none
- changed operator variables: none
- all four new variables validated
- image-inherited variables added/removed/changed on the final retry: none
- container ID changed, proving recreation
- restart policy: `unless-stopped`
- image ID remained `sha256:b18bb0...`

An earlier attempt used the wrong stop criterion and treated an inherited
`NODE_VERSION` difference as project configuration drift. It was rolled back,
including a successful restore of both verified dumps. The corrected
criterion compares operator-set and image-inherited variables separately.

Rollback: stop n8n, replace the live stack with
`n8n-stack.rollback.yml`, and recreate only the n8n service. Restore database
dumps only when actual database damage or credential unreadability is
confirmed.

## 4. Post-recreation verification

### 4a. Runtime

- n8n version: `1.121.3`
- `GET /healthz`: HTTP `200`
- body: `{"status":"ok"}`

### 4b. Inventory

- workflows: 58
- active `API:*`: 0
- inactive, unarchived `API:*`: 35
- credentials: 6

### 4c. Credential decryption

A new temporary, inactive workflow sent a real SMTP email through the existing
credential after recreation.

- execution exit: `0`
- delivery to `alexander@sedamedical.com`: confirmed
- temporary workflow deleted

This proves that the preserved `N8N_ENCRYPTION_KEY` still decrypts the SMTP
credential.

### 4d. Guard capability workflow

A temporary, inactive manual-trigger workflow executed one Code node using:

- `require('crypto')`
- `require('jsonwebtoken')`
- JWT sign and verify with HS256
- `$env.JWT_SIGNING_SECRET`

Safe output:

```text
crypto_ok=true
jwt_sign_verify_ok=true
jwt_secret_length=88
```

The workflow was deleted. No temporary test workflow remained, and the
original expired API-key row was restored.

**Go/no-go verdict:** **GO — the guard design is confirmed buildable on this
instance.**

## 5. New database

Created database `epe_2026` with schema `performance_db` from the live 2025
schema baseline.

Corrections:

- `evaluations.period_id` is `NOT NULL`
- non-self unique index:
  `(subject_id, evaluator_id, evaluation_source, period_id)`
- self unique index: `(subject_id, period_id)`
- `score_corrections.period_id` is `NOT NULL` with a foreign key to
  `evaluation_periods`
- score-correction unique index:
  `(subject_id, criteria_id, correction_level, period_id)`
- `evaluation_periods.period_type` added
- `evaluation_periods.parent_period_id` added with a self-reference foreign key

Copied reference data only:

- criteria: 8
- departments: 14
- grades: 10
- score coefficients: 80

Verified empty:

- users
- evaluations
- evaluation scores
- score corrections
- evaluation periods
- invite tokens
- email verification codes
- global settings

Additional evidence:

- 12 tables in the target schema
- users with non-null `password_hash`: 0
- active connections to `epe_2026`: 0
- n8n `DB_POSTGRESDB_DATABASE`: `postgres`

Rollback: because the database is unused, drop only `epe_2026`. Do not alter
the source `postgres.performance_db` schema.

## Portainer diagnosis

Portainer itself is working:

- container state: running, zero restarts
- local `/api/status`: HTTP 200, Portainer 2.27.1
- `/api/users/admin/check`: HTTP 204, so an admin already exists
- no initial-setup timeout or login failure was found

The access failure was the firewall allowlist:

- former allowed address for port 9000: `212.36.169.90`
- Alexander's current address: `188.137.254.191`
- port 9000 DROP counter: 878 packets

Follow-up:

- the persisted firewall allowlist was changed to `188.137.254.191`
- 12 old-address rules were removed and 12 current-address rules installed
- port 5678 remains outside the restricted chains and publicly returns 200
- the agent shell egresses through `216.147.123.47`, so it cannot directly
  prove access from Alexander's browser address
- SSH alias `epe-vps-tunnel` now forwards remote ports without depending on
  the home-IP allowlist:
  - PostgreSQL 5432 at `127.0.0.1:25432`
  - Portainer 9000 at `http://127.0.0.1:29000`
  - PostgreSQL 5431 at `127.0.0.1:25431`
- launchd job `com.sedamedical.epe-vps-tunnel` keeps the tunnel running and
  restarts it automatically
- all three tunnels opened successfully; Portainer returned HTTP 200 through
  the persistent tunnel

Password reset is not indicated.
