{
  "updatedAt": "2026-08-27T12:43:34.472Z",
  "createdAt": "2026-08-27T11:48:57.317Z",
  "id": "KLDk6WmWZKsZ8GVX",
  "name": "API: Peer Recognition",
  "description": null,
  "active": true,
  "isArchived": false,
  "nodes": [
    {
      "parameters": {
        "httpMethod": "GET",
        "path": "api/recognition/form",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "recognition-webhook-form",
      "name": "Webhook FORM",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        -700,
        0
      ],
      "webhookId": "epe-recognition-form"
    },
    {
      "parameters": {
        "jsCode": "const request = $input.first().json;\nreturn {\n  json: {\n    authorization: request.headers?.authorization || '',\n    required_roles: [],\n    required_capability: \"\",\n    request,\n  },\n};"
      },
      "id": "recognition-guard-input-form",
      "name": "Prepare Guard Input FORM",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -480,
        0
      ]
    },
    {
      "parameters": {
        "workflowId": "L0Zr7nVa8O5YWXd3",
        "options": {}
      },
      "id": "recognition-run-guard-form",
      "name": "Run Auth Guard FORM",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1,
      "position": [
        -250,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "const guard = $('Run Auth Guard FORM').first().json;\nif (!guard.ok) {\n  return {\n    json: {\n      http_status: guard.status,\n      body: { success: false, error: guard.code, message: guard.message },\n    },\n  };\n}\nconst actorId = Number(guard.identity.id);\n\n// One read for the whole page: the period, the actor's own nomination if it\n// exists, and the two candidate lists. `blocked` is returned rather than\n// silently dropped so the screen can say WHY somebody cannot be named — the\n// owner asked for that sentence about a manager specifically.\nreturn {\n  json: {\n    ok: true,\n    sql: `\nWITH current_period AS (\n  SELECT p.id,\n         p.name,\n         to_char(p.start_date, 'YYYY-MM-DD') AS start_date_text,\n         to_char(p.end_date, 'YYYY-MM-DD') AS end_date_text\n  FROM performance_db.evaluation_periods p\n  WHERE p.is_active = true\n    AND p.status = 'active'\n    AND p.period_type <> 'annual'\n    AND NOT EXISTS (SELECT 1 FROM performance_db.evaluation_periods child\n                    WHERE child.parent_period_id = p.id)\n  LIMIT 1\n),\nactor AS (\n  SELECT u.id, u.manager_id, (u.terminated_at IS NOT NULL) AS is_terminated\n  FROM performance_db.users u\n  WHERE u.id = ${actorId}\n),\ncandidates AS (\n  SELECT\n    u.id,\n    u.full_name,\n    u.job_title,\n    d.name AS department_name,\n    CASE\n      WHEN u.id = a.id THEN 'self'\n      WHEN a.manager_id IS NOT NULL AND u.id = a.manager_id THEN 'own_manager'\n      WHEN u.manager_id = a.id THEN 'own_report'\n      ELSE NULL\n    END AS blocked_reason\n  FROM performance_db.users u\n  CROSS JOIN actor a\n  LEFT JOIN performance_db.departments d ON d.id = u.department_id\n  -- A terminated person is not a candidate at all: they are not in the list and\n  -- not in the blocked list either. They have left.\n  WHERE u.terminated_at IS NULL\n),\nmine AS (\n  SELECT\n    r.id,\n    r.nominee_id,\n    n.full_name AS nominee_name,\n    r.situation,\n    r.action,\n    r.outcome,\n    to_char(r.updated_at AT TIME ZONE 'UTC', 'YYYY-MM-DD\"T\"HH24:MI:SS\"Z\"') AS updated_at\n  FROM performance_db.peer_recognitions r\n  JOIN performance_db.users n ON n.id = r.nominee_id\n  JOIN current_period cp ON cp.id = r.period_id\n  WHERE r.author_id = ${actorId}\n  LIMIT 1\n)\nSELECT\n  (SELECT id FROM current_period) AS period_id,\n  (SELECT name FROM current_period) AS period_name,\n  (SELECT start_date_text FROM current_period) AS period_start_date,\n  (SELECT end_date_text FROM current_period) AS period_end_date,\n  (SELECT is_terminated FROM actor) AS actor_terminated,\n  COALESCE((SELECT json_agg(row_to_json(c) ORDER BY c.full_name)\n            FROM candidates c WHERE c.blocked_reason IS NULL), '[]'::json) AS colleagues,\n  COALESCE((SELECT json_agg(row_to_json(c) ORDER BY c.full_name)\n            FROM candidates c WHERE c.blocked_reason IS NOT NULL), '[]'::json) AS blocked,\n  (SELECT row_to_json(m) FROM mine m) AS my_nomination\n    `,\n  },\n};"
      },
      "id": "recognition-form-build",
      "name": "Build Form Query",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.ok ? $json.sql : 'SELECT NULL::integer AS period_id WHERE false' }}",
        "options": {}
      },
      "id": "recognition-form-load",
      "name": "Load Form",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        250,
        0
      ],
      "credentials": {
        "postgres": {
          "id": "VNbfkY8IKbEzn88B",
          "name": "EPE 2026 Postgres"
        }
      },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "const guard = $('Run Auth Guard FORM').first().json;\nif (!guard.ok) {\n  return {\n    json: {\n      http_status: guard.status,\n      body: { success: false, error: guard.code, message: guard.message },\n    },\n  };\n}\nconst BLOCK_MESSAGES = {\"own_manager\": \"Своего руководителя здесь отметить нельзя — для этого есть оценка «снизу вверх» в ваших задачах.\", \"self\": \"Себя отметить нельзя.\", \"own_report\": \"Своего подчинённого здесь отметить нельзя.\"};\nconst parseJson = (value, fallback) => {\n  if (value === null || value === undefined) return fallback;\n  if (typeof value !== 'string') return value;\n  try { return JSON.parse(value); } catch { return fallback; }\n};\nconst row = $input.first().json || {};\nconst colleagues = parseJson(row.colleagues, []);\nconst blockedRaw = parseJson(row.blocked, []);\nconst mine = parseJson(row.my_nomination, null);\n\nconst clean = person => ({\n  id: Number(person.id),\n  full_name: person.full_name,\n  job_title: person.job_title || null,\n  department_name: person.department_name || null,\n});\nconst blocked = (Array.isArray(blockedRaw) ? blockedRaw : []).map(person => ({\n  ...clean(person),\n  blocked_reason: person.blocked_reason,\n  message: BLOCK_MESSAGES[person.blocked_reason] || 'Этого человека здесь отметить нельзя.',\n}));\n\nreturn {\n  json: {\n    http_status: 200,\n    body: {\n      success: true,\n      actor_user_id: Number(guard.identity.id),\n      period: row.period_id\n        ? {\n            id: Number(row.period_id),\n            name: row.period_name || null,\n            start_date: row.period_start_date || null,\n            end_date: row.period_end_date || null,\n          }\n        : null,\n      colleagues: (Array.isArray(colleagues) ? colleagues : []).map(clean),\n      blocked,\n      my_nomination: mine\n        ? {\n            id: Number(mine.id),\n            nominee_id: Number(mine.nominee_id),\n            nominee_name: mine.nominee_name,\n            situation: mine.situation,\n            action: mine.action,\n            outcome: mine.outcome,\n            updated_at: mine.updated_at,\n          }\n        : null,\n    },\n  },\n};"
      },
      "id": "recognition-form-format",
      "name": "Format Form Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        0
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ $json.body }}",
        "options": {
          "responseCode": "={{ $json.http_status }}"
        }
      },
      "id": "recognition-respond-form",
      "name": "Respond FORM",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [
        740,
        0
      ]
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "api/recognition/save",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "recognition-webhook-save",
      "name": "Webhook SAVE",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        -700,
        300
      ],
      "webhookId": "epe-recognition-save"
    },
    {
      "parameters": {
        "jsCode": "const request = $input.first().json;\nreturn {\n  json: {\n    authorization: request.headers?.authorization || '',\n    required_roles: [],\n    required_capability: \"\",\n    request,\n  },\n};"
      },
      "id": "recognition-guard-input-save",
      "name": "Prepare Guard Input SAVE",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -480,
        300
      ]
    },
    {
      "parameters": {
        "workflowId": "L0Zr7nVa8O5YWXd3",
        "options": {}
      },
      "id": "recognition-run-guard-save",
      "name": "Run Auth Guard SAVE",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1,
      "position": [
        -250,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "const guard = $('Run Auth Guard SAVE').first().json;\nif (!guard.ok) {\n  return {\n    json: {\n      http_status: guard.status,\n      body: { success: false, error: guard.code, message: guard.message },\n    },\n  };\n}\nconst actorId = Number(guard.identity.id);\nconst body = guard.request.body || guard.request;\n\nconst nomineeId = parseInt(body.nominee_id, 10);\nif (!Number.isFinite(nomineeId) || nomineeId < 1) {\n  return {\n    json: {\n      http_status: 422,\n      body: { success: false, error: 'INVALID_NOMINEE_ID', message: 'Выберите коллегу из списка' },\n    },\n  };\n}\nconst MAX = 2000;\n// U+0000 cannot be stored in a Postgres text column at all; stripping it here\n// turns a 500 from the driver into an ordinary, saved nomination.\nconst field = value => String(value ?? '').replace(/\\u0000/g, '').trim();\nconst situation = field(body.situation);\nconst action = field(body.action);\nconst outcome = field(body.outcome);\nif (!situation || !action || !outcome) {\n  return {\n    json: {\n      http_status: 422,\n      body: {\n        success: false,\n        error: 'RECOGNITION_TEXT_REQUIRED',\n        message: 'Заполните все три поля: ситуацию, действие и результат',\n      },\n    },\n  };\n}\nif (situation.length > MAX || action.length > MAX || outcome.length > MAX) {\n  return {\n    json: {\n      http_status: 422,\n      body: {\n        success: false,\n        error: 'RECOGNITION_TEXT_TOO_LONG',\n        message: `Каждое поле не длиннее ${MAX} символов`,\n      },\n    },\n  };\n}\n\nreturn {\n  json: {\n    ok: true,\n    actor_id: actorId,\n    nominee_id: nomineeId,\n    situation,\n    action,\n    outcome,\n    sql: `\n      SELECT\n        a.id AS actor_id,\n        a.manager_id AS actor_manager_id,\n        (a.terminated_at IS NOT NULL) AS actor_terminated,\n        n.id AS nominee_id,\n        n.full_name AS nominee_name,\n        n.manager_id AS nominee_manager_id,\n        (n.terminated_at IS NOT NULL) AS nominee_terminated,\n        (SELECT cp.id FROM performance_db.evaluation_periods cp\n          WHERE cp.is_active = true AND cp.status = 'active'\n            AND cp.period_type <> 'annual'\n            AND NOT EXISTS (SELECT 1 FROM performance_db.evaluation_periods child\n                            WHERE child.parent_period_id = cp.id)\n          LIMIT 1) AS period_id,\n        (SELECT r.id FROM performance_db.peer_recognitions r\n          WHERE r.author_id = a.id\n            AND r.period_id = (SELECT cp.id FROM performance_db.evaluation_periods cp\n              WHERE cp.is_active = true AND cp.status = 'active'\n                AND cp.period_type <> 'annual'\n                AND NOT EXISTS (SELECT 1 FROM performance_db.evaluation_periods child\n                                WHERE child.parent_period_id = cp.id)\n              LIMIT 1)) AS existing_id\n      FROM performance_db.users a\n      LEFT JOIN performance_db.users n ON n.id = ${nomineeId}\n      WHERE a.id = ${actorId}\n    `,\n  },\n};"
      },
      "id": "recognition-save-validate",
      "name": "Validate Save",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        0,
        300
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.ok ? $json.sql : 'SELECT NULL::integer AS actor_id WHERE false' }}",
        "options": {}
      },
      "id": "recognition-save-check",
      "name": "Load Save Target",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        250,
        300
      ],
      "credentials": {
        "postgres": {
          "id": "VNbfkY8IKbEzn88B",
          "name": "EPE 2026 Postgres"
        }
      },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "const prev = $('Validate Save').first().json;\nif (prev.http_status) {\n  return { json: prev };\n}\nconst BLOCK_MESSAGES = {\"own_manager\": \"Своего руководителя здесь отметить нельзя — для этого есть оценка «снизу вверх» в ваших задачах.\", \"self\": \"Себя отметить нельзя.\", \"own_report\": \"Своего подчинённого здесь отметить нельзя.\"};\nconst check = $input.all().map(item => item.json).find(item => item.actor_id !== undefined);\nif (!check) {\n  // The actor's own row is gone. The guard read it a moment ago, so this is a\n  // race, not a normal state.\n  return {\n    json: {\n      http_status: 404,\n      body: { success: false, error: 'ACTOR_NOT_FOUND', message: 'Ваша учётная запись не найдена' },\n    },\n  };\n}\nconst truthy = value => value === true || value === 't';\nconst actorId = Number(prev.actor_id);\nconst nomineeId = Number(prev.nominee_id);\n\n// Refusal order, most-general first: no period at all → the actor is not\n// allowed to write → the nominee does not exist → the three relationship\n// refusals → the nominee has left.\nif (check.period_id === null || check.period_id === undefined) {\n  return {\n    json: {\n      http_status: 409,\n      body: {\n        success: false,\n        error: 'NO_ACTIVE_PERIOD',\n        message: 'Сейчас нет открытого периода — отметить коллегу можно только внутри периода',\n      },\n    },\n  };\n}\nif (truthy(check.actor_terminated)) {\n  return {\n    json: {\n      http_status: 403,\n      body: { success: false, error: 'ACTOR_TERMINATED', message: 'Учётная запись неактивна' },\n    },\n  };\n}\nif (check.nominee_id === null || check.nominee_id === undefined) {\n  return {\n    json: {\n      http_status: 404,\n      body: { success: false, error: 'NOMINEE_NOT_FOUND', message: 'Сотрудник не найден' },\n    },\n  };\n}\nif (nomineeId === actorId) {\n  return {\n    json: {\n      http_status: 422,\n      body: { success: false, error: 'RECOGNITION_SELF', message: BLOCK_MESSAGES.self },\n    },\n  };\n}\nif (check.actor_manager_id !== null && check.actor_manager_id !== undefined\n    && Number(check.actor_manager_id) === nomineeId) {\n  return {\n    json: {\n      http_status: 422,\n      body: { success: false, error: 'RECOGNITION_OWN_MANAGER', message: BLOCK_MESSAGES.own_manager },\n    },\n  };\n}\nif (check.nominee_manager_id !== null && check.nominee_manager_id !== undefined\n    && Number(check.nominee_manager_id) === actorId) {\n  return {\n    json: {\n      http_status: 422,\n      body: { success: false, error: 'RECOGNITION_OWN_REPORT', message: BLOCK_MESSAGES.own_report },\n    },\n  };\n}\nif (truthy(check.nominee_terminated)) {\n  return {\n    json: {\n      http_status: 422,\n      body: {\n        success: false,\n        error: 'NOMINEE_TERMINATED',\n        message: 'Этот сотрудник больше не работает в компании',\n      },\n    },\n  };\n}\n\nconst periodId = Number(check.period_id);\nconst quote = value => `'${String(value).replace(/'/g, \"''\")}'`;\n// Cosmetic only: it decides «сохранена» vs «обновлена». The uniqueness that\n// makes \"exactly one\" true is the constraint, not this read.\nconst replaced = check.existing_id !== null && check.existing_id !== undefined;\n\n// One statement. Every refusal above is re-asserted inside `target`, so a body\n// hand-made against a graph that changed a moment ago writes zero rows instead\n// of writing the wrong one.\nreturn {\n  json: {\n    ok: true,\n    nominee_name: check.nominee_name,\n    replaced,\n    sql: `\nWITH target AS (\n  SELECT p.id AS period_id, a.id AS author_id, n.id AS nominee_id\n  FROM performance_db.evaluation_periods p\n  JOIN performance_db.users a\n    ON a.id = ${actorId} AND a.terminated_at IS NULL\n  JOIN performance_db.users n\n    ON n.id = ${nomineeId} AND n.terminated_at IS NULL\n  WHERE p.id = ${periodId}\n    AND p.is_active = true\n    AND p.status = 'active'\n    AND n.id <> a.id\n    AND (a.manager_id IS NULL OR n.id <> a.manager_id)\n    AND (n.manager_id IS NULL OR n.manager_id <> a.id)\n),\nsaved AS (\n  INSERT INTO performance_db.peer_recognitions\n    (period_id, author_id, nominee_id, situation, action, outcome)\n  SELECT t.period_id, t.author_id, t.nominee_id,\n         ${quote(prev.situation)}, ${quote(prev.action)}, ${quote(prev.outcome)}\n  FROM target t\n  ON CONFLICT ON CONSTRAINT uq_peer_recognitions_period_author DO UPDATE\n    SET nominee_id = EXCLUDED.nominee_id,\n        situation  = EXCLUDED.situation,\n        action     = EXCLUDED.action,\n        outcome    = EXCLUDED.outcome,\n        updated_at = now()\n  RETURNING id, nominee_id, period_id\n)\nSELECT\n  (SELECT count(*)::integer FROM saved) AS written,\n  (SELECT id FROM saved LIMIT 1) AS recognition_id,\n  (SELECT period_id FROM saved LIMIT 1) AS period_id\n    `,\n  },\n};"
      },
      "id": "recognition-save-build",
      "name": "Build Save SQL",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        300
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.ok ? $json.sql : 'SELECT NULL::integer AS written WHERE false' }}",
        "options": {}
      },
      "id": "recognition-save-execute",
      "name": "Execute Save",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        750,
        300
      ],
      "credentials": {
        "postgres": {
          "id": "VNbfkY8IKbEzn88B",
          "name": "EPE 2026 Postgres"
        }
      },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "const prev = $('Build Save SQL').first().json;\nif (prev.http_status) {\n  return { json: prev };\n}\nconst row = $input.all().map(item => item.json).find(item => item.written !== undefined);\nif (!row || Number(row.written) === 0) {\n  return {\n    json: {\n      http_status: 409,\n      body: {\n        success: false,\n        error: 'RECOGNITION_CONFLICT',\n        message: 'Состояние изменилось во время сохранения — обновите страницу и повторите',\n      },\n    },\n  };\n}\nreturn {\n  json: {\n    http_status: 200,\n    body: {\n      success: true,\n      recognition_id: Number(row.recognition_id),\n      period_id: Number(row.period_id),\n      nominee_name: prev.nominee_name,\n      replaced: prev.replaced === true,\n      message: prev.replaced === true ? 'Отметка обновлена' : 'Отметка сохранена',\n    },\n  },\n};"
      },
      "id": "recognition-save-format",
      "name": "Format Save Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1000,
        300
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ $json.body }}",
        "options": {
          "responseCode": "={{ $json.http_status }}"
        }
      },
      "id": "recognition-respond-save",
      "name": "Respond SAVE",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [
        1240,
        300
      ]
    },
    {
      "parameters": {
        "httpMethod": "GET",
        "path": "api/recognition/list",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "recognition-webhook-list",
      "name": "Webhook LIST",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        -700,
        600
      ],
      "webhookId": "epe-recognition-list"
    },
    {
      "parameters": {
        "jsCode": "const request = $input.first().json;\nreturn {\n  json: {\n    authorization: request.headers?.authorization || '',\n    required_roles: [\"admin\", \"c_level\"],\n    required_capability: \"\",\n    request,\n  },\n};"
      },
      "id": "recognition-guard-input-list",
      "name": "Prepare Guard Input LIST",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -480,
        600
      ]
    },
    {
      "parameters": {
        "workflowId": "L0Zr7nVa8O5YWXd3",
        "options": {}
      },
      "id": "recognition-run-guard-list",
      "name": "Run Auth Guard LIST",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1,
      "position": [
        -250,
        600
      ]
    },
    {
      "parameters": {
        "jsCode": "const guard = $('Run Auth Guard LIST').first().json;\nif (!guard.ok) {\n  return {\n    json: {\n      http_status: guard.status,\n      body: { success: false, error: guard.code, message: guard.message },\n    },\n  };\n}\nconst query = (guard.request || {}).query || {};\nconst requested = parseInt(query.period_id ?? query.periodId, 10);\nconst periodExpression = Number.isFinite(requested) && requested > 0\n  ? String(requested)\n  : `(SELECT cp.id FROM performance_db.evaluation_periods cp\n      WHERE cp.is_active = true AND cp.status = 'active'\n        AND cp.period_type <> 'annual'\n        AND NOT EXISTS (SELECT 1 FROM performance_db.evaluation_periods child\n                        WHERE child.parent_period_id = cp.id)\n      LIMIT 1)`;\n\n// Newest first, by time alone. There is deliberately no GROUP BY, no count and\n// no ordering that could be read as a ranking: the moment this list can be\n// sorted by how often somebody was named it becomes the popularity contest the\n// whole design exists to avoid.\nreturn {\n  json: {\n    ok: true,\n    sql: `\n      SELECT\n        r.id,\n        r.period_id,\n        p.name AS period_name,\n        r.author_id,\n        au.full_name AS author_name,\n        au.job_title AS author_job_title,\n        ad.name AS author_department,\n        r.nominee_id,\n        nu.full_name AS nominee_name,\n        nu.job_title AS nominee_job_title,\n        nd.name AS nominee_department,\n        r.situation,\n        r.action,\n        r.outcome,\n        to_char(r.created_at AT TIME ZONE 'UTC', 'YYYY-MM-DD\"T\"HH24:MI:SS\"Z\"') AS created_at,\n        to_char(r.updated_at AT TIME ZONE 'UTC', 'YYYY-MM-DD\"T\"HH24:MI:SS\"Z\"') AS updated_at\n      FROM performance_db.peer_recognitions r\n      JOIN performance_db.users au ON au.id = r.author_id\n      JOIN performance_db.users nu ON nu.id = r.nominee_id\n      JOIN performance_db.evaluation_periods p ON p.id = r.period_id\n      LEFT JOIN performance_db.departments ad ON ad.id = au.department_id\n      LEFT JOIN performance_db.departments nd ON nd.id = nu.department_id\n      WHERE r.period_id = ${periodExpression}\n      ORDER BY r.created_at DESC, r.id DESC\n      LIMIT 500\n    `,\n  },\n};"
      },
      "id": "recognition-list-build",
      "name": "Build List Query",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        0,
        600
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.ok ? $json.sql : 'SELECT NULL::integer AS id WHERE false' }}",
        "options": {}
      },
      "id": "recognition-list-load",
      "name": "Load List",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        250,
        600
      ],
      "credentials": {
        "postgres": {
          "id": "VNbfkY8IKbEzn88B",
          "name": "EPE 2026 Postgres"
        }
      },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "const guard = $('Run Auth Guard LIST').first().json;\nif (!guard.ok) {\n  return {\n    json: {\n      http_status: guard.status,\n      body: { success: false, error: guard.code, message: guard.message },\n    },\n  };\n}\nconst items = $input.all().map(item => item.json).filter(item => item.id !== undefined);\nreturn {\n  json: {\n    http_status: 200,\n    body: {\n      success: true,\n      period_id: items.length ? Number(items[0].period_id) : null,\n      period_name: items.length ? items[0].period_name : null,\n      recognitions: items,\n    },\n  },\n};"
      },
      "id": "recognition-list-format",
      "name": "Format List Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        600
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ $json.body }}",
        "options": {
          "responseCode": "={{ $json.http_status }}"
        }
      },
      "id": "recognition-respond-list",
      "name": "Respond LIST",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [
        740,
        600
      ]
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "api/recognition/withdraw",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "recognition-webhook-withdraw",
      "name": "Webhook WITHDRAW",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        -700,
        900
      ],
      "webhookId": "epe-recognition-withdraw"
    },
    {
      "parameters": {
        "jsCode": "const request = $input.first().json;\nreturn {\n  json: {\n    authorization: request.headers?.authorization || '',\n    required_roles: [],\n    required_capability: \"\",\n    request,\n  },\n};"
      },
      "id": "recognition-guard-input-withdraw",
      "name": "Prepare Guard Input WITHDRAW",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -480,
        900
      ]
    },
    {
      "parameters": {
        "workflowId": "L0Zr7nVa8O5YWXd3",
        "options": {}
      },
      "id": "recognition-run-guard-withdraw",
      "name": "Run Auth Guard WITHDRAW",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1,
      "position": [
        -250,
        900
      ]
    },
    {
      "parameters": {
        "jsCode": "const guard = $('Run Auth Guard WITHDRAW').first().json;\nif (!guard.ok) {\n  return {\n    json: {\n      http_status: guard.status,\n      body: { success: false, error: guard.code, message: guard.message },\n    },\n  };\n}\nconst actorId = Number(guard.identity.id);\nconst body = guard.request.body || guard.request;\nconst recognitionId = parseInt(body.recognition_id, 10);\nif (!Number.isFinite(recognitionId) || recognitionId < 1) {\n  return {\n    json: {\n      http_status: 422,\n      body: {\n        success: false,\n        error: 'INVALID_RECOGNITION_ID',\n        message: 'Не указана отметка',\n      },\n    },\n  };\n}\n\n// Identity is the token actor. A client-supplied author_id is ignored: the\n// only row this route can ever delete is the actor's own.\nreturn {\n  json: {\n    ok: true,\n    actor_id: actorId,\n    recognition_id: recognitionId,\n    sql: `\n      SELECT\n        a.id AS actor_id,\n        (a.terminated_at IS NOT NULL) AS actor_terminated,\n        (SELECT cp.id FROM performance_db.evaluation_periods cp\n          WHERE cp.is_active = true AND cp.status = 'active'\n            AND cp.period_type <> 'annual'\n            AND NOT EXISTS (SELECT 1 FROM performance_db.evaluation_periods child\n                            WHERE child.parent_period_id = cp.id)\n          LIMIT 1) AS period_id,\n        r.id AS recognition_id,\n        r.author_id AS row_author_id,\n        r.period_id AS row_period_id\n      FROM performance_db.users a\n      LEFT JOIN performance_db.peer_recognitions r ON r.id = ${recognitionId}\n      WHERE a.id = ${actorId}\n    `,\n  },\n};"
      },
      "id": "recognition-withdraw-validate",
      "name": "Validate Withdraw",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        0,
        900
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.ok ? $json.sql : 'SELECT NULL::integer AS actor_id WHERE false' }}",
        "options": {}
      },
      "id": "recognition-withdraw-check",
      "name": "Load Withdraw Target",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        250,
        900
      ],
      "credentials": {
        "postgres": {
          "id": "VNbfkY8IKbEzn88B",
          "name": "EPE 2026 Postgres"
        }
      },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "const prev = $('Validate Withdraw').first().json;\nif (prev.http_status) {\n  return { json: prev };\n}\nconst check = $input.all().map(item => item.json).find(item => item.actor_id !== undefined);\nif (!check) {\n  return {\n    json: {\n      http_status: 404,\n      body: { success: false, error: 'ACTOR_NOT_FOUND', message: 'Ваша учётная запись не найдена' },\n    },\n  };\n}\nconst truthy = value => value === true || value === 't';\nconst actorId = Number(prev.actor_id);\nconst recognitionId = Number(prev.recognition_id);\n\n// Same gate as replace: no active leaf → 409. A closed period keeps the row.\nif (check.period_id === null || check.period_id === undefined) {\n  return {\n    json: {\n      http_status: 409,\n      body: {\n        success: false,\n        error: 'NO_ACTIVE_PERIOD',\n        message: 'Сейчас нет открытого периода — отметить коллегу можно только внутри периода',\n      },\n    },\n  };\n}\nif (truthy(check.actor_terminated)) {\n  return {\n    json: {\n      http_status: 403,\n      body: { success: false, error: 'ACTOR_TERMINATED', message: 'Учётная запись неактивна' },\n    },\n  };\n}\nif (check.recognition_id === null || check.recognition_id === undefined) {\n  return {\n    json: {\n      http_status: 404,\n      body: { success: false, error: 'RECOGNITION_NOT_FOUND', message: 'Отметки нет' },\n    },\n  };\n}\nif (Number(check.row_author_id) !== actorId) {\n  return {\n    json: {\n      http_status: 403,\n      body: {\n        success: false,\n        error: 'RECOGNITION_NOT_OWN',\n        message: 'Снять можно только свою отметку',\n      },\n    },\n  };\n}\nif (Number(check.row_period_id) !== Number(check.period_id)) {\n  return {\n    json: {\n      http_status: 409,\n      body: {\n        success: false,\n        error: 'NO_ACTIVE_PERIOD',\n        message: 'Сейчас нет открытого периода — отметить коллегу можно только внутри периода',\n      },\n    },\n  };\n}\n\nconst periodId = Number(check.period_id);\n// One statement. Author identity and the open leaf are re-asserted inside\n// the DELETE, so a hand-made body against a row that is not the actor's\n// writes zero rows (BUG-041).\nreturn {\n  json: {\n    ok: true,\n    sql: `\nWITH current_period AS (\n  SELECT p.id\n  FROM performance_db.evaluation_periods p\n  WHERE p.id = ${periodId}\n    AND p.is_active = true\n    AND p.status = 'active'\n    AND p.period_type <> 'annual'\n    AND NOT EXISTS (SELECT 1 FROM performance_db.evaluation_periods child\n                    WHERE child.parent_period_id = p.id)\n),\ndeleted AS (\n  DELETE FROM performance_db.peer_recognitions r\n  USING current_period cp, performance_db.users a\n  WHERE r.id = ${recognitionId}\n    AND r.author_id = ${actorId}\n    AND r.period_id = cp.id\n    AND a.id = ${actorId}\n    AND a.terminated_at IS NULL\n  RETURNING r.id\n)\nSELECT\n  (SELECT count(*)::integer FROM deleted) AS written,\n  (SELECT id FROM deleted LIMIT 1) AS recognition_id\n    `,\n  },\n};"
      },
      "id": "recognition-withdraw-build",
      "name": "Build Withdraw SQL",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        900
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.ok ? $json.sql : 'SELECT NULL::integer AS written WHERE false' }}",
        "options": {}
      },
      "id": "recognition-withdraw-execute",
      "name": "Execute Withdraw",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        750,
        900
      ],
      "credentials": {
        "postgres": {
          "id": "VNbfkY8IKbEzn88B",
          "name": "EPE 2026 Postgres"
        }
      },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "const prev = $('Build Withdraw SQL').first().json;\nif (prev.http_status) {\n  return { json: prev };\n}\nconst row = $input.all().map(item => item.json).find(item => item.written !== undefined);\nif (!row || Number(row.written) === 0) {\n  return {\n    json: {\n      http_status: 409,\n      body: {\n        success: false,\n        error: 'RECOGNITION_CONFLICT',\n        message: 'Состояние изменилось во время сохранения — обновите страницу и повторите',\n      },\n    },\n  };\n}\nreturn {\n  json: {\n    http_status: 200,\n    body: {\n      success: true,\n      withdrawn: true,\n      recognition_id: Number(row.recognition_id),\n      message: 'Отметка снята',\n    },\n  },\n};"
      },
      "id": "recognition-withdraw-format",
      "name": "Format Withdraw Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1000,
        900
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ $json.body }}",
        "options": {
          "responseCode": "={{ $json.http_status }}"
        }
      },
      "id": "recognition-respond-withdraw",
      "name": "Respond WITHDRAW",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [
        1240,
        900
      ]
    }
  ],
  "connections": {
    "Webhook FORM": {
      "main": [
        [
          {
            "node": "Prepare Guard Input FORM",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Guard Input FORM": {
      "main": [
        [
          {
            "node": "Run Auth Guard FORM",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run Auth Guard FORM": {
      "main": [
        [
          {
            "node": "Build Form Query",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Form Query": {
      "main": [
        [
          {
            "node": "Load Form",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Form": {
      "main": [
        [
          {
            "node": "Format Form Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format Form Response": {
      "main": [
        [
          {
            "node": "Respond FORM",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook SAVE": {
      "main": [
        [
          {
            "node": "Prepare Guard Input SAVE",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Guard Input SAVE": {
      "main": [
        [
          {
            "node": "Run Auth Guard SAVE",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run Auth Guard SAVE": {
      "main": [
        [
          {
            "node": "Validate Save",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate Save": {
      "main": [
        [
          {
            "node": "Load Save Target",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Save Target": {
      "main": [
        [
          {
            "node": "Build Save SQL",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Save SQL": {
      "main": [
        [
          {
            "node": "Execute Save",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Execute Save": {
      "main": [
        [
          {
            "node": "Format Save Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format Save Response": {
      "main": [
        [
          {
            "node": "Respond SAVE",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook LIST": {
      "main": [
        [
          {
            "node": "Prepare Guard Input LIST",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Guard Input LIST": {
      "main": [
        [
          {
            "node": "Run Auth Guard LIST",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run Auth Guard LIST": {
      "main": [
        [
          {
            "node": "Build List Query",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build List Query": {
      "main": [
        [
          {
            "node": "Load List",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load List": {
      "main": [
        [
          {
            "node": "Format List Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format List Response": {
      "main": [
        [
          {
            "node": "Respond LIST",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook WITHDRAW": {
      "main": [
        [
          {
            "node": "Prepare Guard Input WITHDRAW",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Guard Input WITHDRAW": {
      "main": [
        [
          {
            "node": "Run Auth Guard WITHDRAW",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run Auth Guard WITHDRAW": {
      "main": [
        [
          {
            "node": "Validate Withdraw",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate Withdraw": {
      "main": [
        [
          {
            "node": "Load Withdraw Target",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Withdraw Target": {
      "main": [
        [
          {
            "node": "Build Withdraw SQL",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Withdraw SQL": {
      "main": [
        [
          {
            "node": "Execute Withdraw",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Execute Withdraw": {
      "main": [
        [
          {
            "node": "Format Withdraw Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format Withdraw Response": {
      "main": [
        [
          {
            "node": "Respond WITHDRAW",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveDataErrorExecution": "none",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": false,
    "callerPolicy": "workflowsFromSameOwner",
    "availableInMCP": false
  },
  "staticData": null,
  "meta": null,
  "pinData": null,
  "versionId": "596a4a6d-7a09-4fc0-929f-e5be05a31c3d",
  "versionCounter": 5,
  "triggerCount": 4,
  "shared": [
    {
      "updatedAt": "2026-08-27T11:48:57.317Z",
      "createdAt": "2026-08-27T11:48:57.317Z",
      "role": "workflow:owner",
      "workflowId": "KLDk6WmWZKsZ8GVX",
      "projectId": "etgqxHLuSapoTyiP",
      "project": {
        "updatedAt": "2025-06-21T12:16:10.334Z",
        "createdAt": "2025-06-21T12:14:12.509Z",
        "id": "etgqxHLuSapoTyiP",
        "name": "Aleksandr admin\\ <admin@admin.ru>",
        "type": "personal",
        "icon": null,
        "description": null,
        "projectRelations": [
          {
            "updatedAt": "2025-06-21T12:14:12.509Z",
            "createdAt": "2025-06-21T12:14:12.509Z",
            "userId": "30fb4110-3e17-486d-8374-e261d4b5bb3a",
            "projectId": "etgqxHLuSapoTyiP",
            "user": {
              "updatedAt": "2026-08-27T07:56:38.585Z",
              "createdAt": "2025-06-21T12:14:11.974Z",
              "id": "30fb4110-3e17-486d-8374-e261d4b5bb3a",
              "email": "admin@admin.ru",
              "firstName": "Aleksandr",
              "lastName": "admin\\",
              "personalizationAnswers": {
                "version": "v4",
                "personalization_survey_submitted_at": "2025-06-21T12:16:14.744Z",
                "personalization_survey_n8n_version": "1.98.2"
              },
              "settings": {
                "userActivated": true,
                "easyAIWorkflowOnboarded": true,
                "firstSuccessfulWorkflowId": "VZkADNbuEeJgTBQk",
                "userActivatedAt": 1754208251036,
                "dismissedCallouts": {
                  "aiAgentStarterCallout": true
                },
                "npsSurvey": {
                  "waitingForResponse": true,
                  "ignoredCount": 1,
                  "lastShownAt": 1786569530276
                }
              },
              "disabled": false,
              "mfaEnabled": false,
              "lastActiveAt": "2026-08-27",
              "isPending": false
            }
          }
        ]
      }
    }
  ],
  "tags": []
}
