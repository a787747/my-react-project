{
  "name": "API: Reset Password",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "api/reset-password",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "reset-password-webhook",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        -700,
        0
      ],
      "webhookId": "epe-reset-password"
    },
    {
      "parameters": {
        "jsCode": "const crypto = require('crypto');\nconst body = $input.first().json.body || $input.first().json;\nconst token = String(body.token || '').trim();\nconst password = String(body.password || '');\nconst tokenValid = /^[A-Za-z0-9_-]{40,128}$/.test(token);\nconst passwordValid = password.length >= 8;\n\nif (!tokenValid || !passwordValid) {\n  return {\n    json: {\n      input_valid: false,\n      token_hash: '0'.repeat(64),\n      password,\n    },\n  };\n}\n\nreturn {\n  json: {\n    input_valid: true,\n    token_hash: crypto.createHash('sha256').update(token).digest('hex'),\n    password,\n  },\n};"
      },
      "id": "reset-password-validate",
      "name": "Validate Reset",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -480,
        0
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "=SELECT\n  reset.id AS reset_id,\n  reset.user_id,\n  users.email,\n  users.token_version\nFROM performance_db.password_reset_tokens reset\nJOIN performance_db.users users ON users.id = reset.user_id\nWHERE reset.token_hash = '{{ $json.token_hash }}'\n  AND reset.used_at IS NULL\n  AND reset.expires_at > now()\nLIMIT 1",
        "options": {}
      },
      "id": "reset-password-load",
      "name": "Load Reset Token",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        -250,
        0
      ],
      "credentials": {
        "postgres": {
          "id": "VNbfkY8IKbEzn88B",
          "name": "EPE 2026 Postgres"
        }
      },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "const crypto = require('crypto');\nconst input = $('Validate Reset').first().json;\nconst row = $input.first().json;\n\nif (!input.input_valid || !row.reset_id || !row.user_id) {\n  return {\n    json: {\n      sql: 'SELECT false AS password_reset',\n      http_status: 400,\n      body: {\n        success: false,\n        message: 'Reset link is invalid or expired',\n      },\n    },\n  };\n}\n\nconst salt = crypto.randomBytes(16);\nconst derived = crypto.scryptSync(\n  input.password,\n  salt,\n  64,\n  { N: 16384, r: 8, p: 1, maxmem: 64 * 1024 * 1024 },\n);\nconst passwordHash = [\n  '',\n  'scrypt',\n  'N=16384,r=8,p=1',\n  salt.toString('base64url'),\n  derived.toString('base64url'),\n].join('$');\n\nconst sql = `\n  WITH updated_user AS (\n    UPDATE performance_db.users\n    SET password_hash = '${passwordHash}',\n        token_version = token_version + 1\n    WHERE id = ${row.user_id}\n    RETURNING id, token_version\n  ),\n  used_token AS (\n    UPDATE performance_db.password_reset_tokens\n    SET used_at = now()\n    WHERE id = ${row.reset_id}\n      AND used_at IS NULL\n    RETURNING id\n  ),\n  revoked_sessions AS (\n    UPDATE performance_db.auth_sessions\n    SET revoked_at = now()\n    WHERE user_id = ${row.user_id}\n      AND revoked_at IS NULL\n    RETURNING jti\n  )\n  SELECT\n    updated_user.id,\n    updated_user.token_version,\n    EXISTS(SELECT 1 FROM used_token) AS token_used,\n    (SELECT count(*) FROM revoked_sessions) AS sessions_revoked\n  FROM updated_user\n`;\n\nreturn {\n  json: {\n    sql,\n    http_status: 200,\n  },\n};"
      },
      "id": "reset-password-hash",
      "name": "Hash New Password",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.sql }}",
        "options": {}
      },
      "id": "reset-password-persist",
      "name": "Persist Password Reset",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        250,
        0
      ],
      "credentials": {
        "postgres": {
          "id": "VNbfkY8IKbEzn88B",
          "name": "EPE 2026 Postgres"
        }
      },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "const prepared = $('Hash New Password').first().json;\nconst row = $input.first().json;\nconst success = Boolean(row.id && row.token_used);\nreturn {\n  json: success\n    ? {\n        http_status: 200,\n        body: {\n          success: true,\n          message: 'Password reset successful. Please sign in again.',\n        },\n      }\n    : {\n        http_status: prepared.http_status || 400,\n        body: prepared.body || {\n          success: false,\n          message: 'Reset link is invalid or expired',\n        },\n      },\n};"
      },
      "id": "reset-password-format",
      "name": "Format Reset Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        0
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ $json.body }}",
        "options": {
          "responseCode": "={{ $json.http_status }}"
        }
      },
      "id": "reset-password-respond",
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [
        740,
        0
      ]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Validate Reset",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate Reset": {
      "main": [
        [
          {
            "node": "Load Reset Token",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Reset Token": {
      "main": [
        [
          {
            "node": "Hash New Password",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Hash New Password": {
      "main": [
        [
          {
            "node": "Persist Password Reset",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Persist Password Reset": {
      "main": [
        [
          {
            "node": "Format Reset Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format Reset Response": {
      "main": [
        [
          {
            "node": "Respond",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveDataErrorExecution": "none",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": false
  }
}
